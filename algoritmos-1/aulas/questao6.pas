program exer6;
var
	inicial:char;
	idade:integer;
begin
	read(inicial);
	read(idade);
	write(idade, ', ', inicial);
end.
//EXERCICIO: ler a idade e a inicial do nome 
//e escrever este valores separados por virgula.
